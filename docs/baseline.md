# 📏 BONDSHIFT 改版前基线

📅记录日期：2026年8月18日。  
🔖Git基线提交：`43e8db8 prepare BondShift classroom demo`。  
🌐线上演示：https://6a203aa86a577d55a1cb4754--bondshift-demo.netlify.app/

## 🧪 构建基线

✅`npm run build`执行成功。  
⏱️本次本地构建耗时约453毫秒。  
📦`dist/index.html`为0.57KB，gzip后为0.38KB。  
🎨主CSS为30.95KB，gzip后为6.23KB。  
📜主JavaScript为514.92KB，gzip后为167.17KB。  
⚠️Vite报告主JavaScript超过500KB，并建议进行代码拆分。

## 🖼️ 图片资源基线

📦六张头像总计为12,030,343字节，约11.47MiB。  
🖼️`golden_retriever.png`为2,231,916字节。  
🖼️`childhood_friend.png`为2,064,419字节。  
🖼️`artist.png`为2,023,362字节。  
🖼️`mature_gentleman.png`为1,927,842字节。  
🖼️`cold_senior.png`为1,901,458字节。  
🖼️`cold_ceo.png`为1,881,346字节。  
⚠️首页会渲染当前男友与多位推荐男友，因此首次访问可能请求多张大图。

## 🧹 代码质量基线

❌`npm run lint`执行失败。  
❌当前共有6个错误和2个警告。  
❌`src/core/avatarEngine.ts`有3个未使用参数错误。  
❌`ChatPage.tsx`、`HomePage.tsx`和`SwapPage.tsx`各有1个`any`类型错误。  
⚠️`ReportPage.tsx`有2个不必要Hook依赖警告。

## 🧭 首次体验基线

❌根路由直接进入`/home`。  
❌新用户默认绑定年下男友。  
❌首次用户不会先看到产品定位或换乘机制解释。  
❌详细偏好设置包含16个MBTI选项和6个属性，首次认知负担较高。  
❌首次对话没有真实大模型能力证明。  
❌用户无法在首次体验中看到两个男友对同一问题的差异。

## 🧠 数据可靠性基线

❌聊天消息和用户记忆目前是全局状态。  
❌不同男友没有独立会话线程。  
❌新用户默认继承3条示例换乘记录。  
❌关系评分使用随机增量。  
❌换乘后的关系重算没有先过滤当前男友的交互记录。  
❌埋点事件只存在前端内存缓冲区。

## 🚧 功能完整度基线

⚠️聊天引擎明确使用模板和修饰器模拟AI。  
⚠️语音页面展示固定的“通话中”状态，但没有真实通话。  
⚠️VR页面仍显示占位组件。  
⚠️换乘对比页面仍显示占位组件。  
⚠️设置页存在多个没有实际后续流程的入口。

## 🎯 8月31日对比目标

🎯主JavaScript目标低于300KB，并通过路由拆分降低首屏执行成本。  
🎯首屏图片传输目标低于1.5MB。  
🎯`npm run build`和`npm run lint`均应成功。  
🎯新用户能够在3至5分钟内完成首次换乘。  
🎯不同男友的聊天、记忆和关系状态必须完全隔离。  
🎯主流程不再展示任何占位功能或虚构用户数据。  
🎯公开测试必须能够记录完整漏斗数据。

## 📈 2026年8月20日第一次优化结果

✅主入口JavaScript降至246.74KB，gzip后为80.25KB，已达到低于300KB目标。  
✅主要页面已按路由拆包，单页异步包约6KB至14KB。  
✅六张实际使用头像合计约460KB，已达到首屏图片低于1.5MB目标。  
✅`npm run build`和`npm run lint`均成功，Lint为0错误、0警告。  
✅首页只保留“聊天”和“换乘”两个真实快捷入口。  
✅虚构社会证明和默认三条换乘历史已移除。  
✅390像素移动端首页没有横向溢出，聊天页和换乘页均能正常加载。  
✅旧`/compare`链接已验证会返回`/home`，浏览器控制台没有错误。
✅数据模型已升级为按男友隔离消息、记忆、互动历史和关系计算。  
✅持久化结构升级到v2，刷新后会话仍可恢复，并兼容v1单份记忆迁移。  
✅关系综合分在相同历史下刷新前后均为31，确定性验证通过。

## 📸 待补充视觉证据

✅已经补充13张桌面端和移动端改版前截图。  
✅首页、三步偏好、聊天、换乘、报告、语音和VR均已有证据。  
✅无法访问的对比占位页截图已由`mobile-swap-confirm.jpg`替代，记录换乘确认流程的问题。  
📌详细清单与观察结论见`docs/evidence/before/README.md`。

## 🧾 2026年8月28日收尾复核待办

- ⚠️`package.json`仍声明未被`src/`引用的`recharts`，与PRD“源码未使用recharts”的事实口径容易混淆；依赖冻结期间不处理，9月2日上线后经确认再清理。
- ⚠️Node 26构建会出现`DEP0205 module.register()`弃用警告，但当前Lint、TypeScript与Vite生产构建均通过；留待下次Node/Vite升级窗口复核。
- ✅监督人已确认Netlify `DEEPSEEK_API_KEY`完成配置；最终生产冒烟仍需在D5阶段复核真实AI回复与日志安全。

## ✅ 2026年8月29日响应式质量结果

- ✅响应式证据达到24/24张标准断点截图，另保留`chat-1920.png`，合计25张。
- ✅320/768/1440三档首次体验全流程通过，情景题可加载、选择、提交并稳定得到顾怀瑾90%匹配。
- ✅999↔1025px横跳10轮通过，SideNav与BottomNav始终互斥，未出现激活指示条残影。
- ✅原有首次体验与桌面响应式两套E2E均通过，控制台错误与页面错误为0。
- ✅主JavaScript为237.64KB，gzip后76.48KB；woff2合计165,048字节（161.18KB）。
- ⚠️`DEP0205`经`--trace-deprecation`定位到`@tailwindcss/node@4.3.0`；该警告不影响构建，依赖冻结期间不升级，留待D5后单独验证4.3.1及以上版本。

## ⚠️ 2026年8月31日已知性能问题

- ⚠️移动端 LCP ≈3.3s（门槛 2.5s），M3 fail-fast 于 8/31 生效；优化候选：主包代码分割、createLucideIcon 121KB chunk 按页分割；排期 D5 后专项。

## 🚀 2026年9月3日生产上线基线（2026年9月4日复验）

- 🌐生产主域名为 `https://bondshift-demo.netlify.app/`，生产页面返回 HTTP 200。
- 🔖生产代码对应 `main` 分支热修复提交 `fbe8d006e7b03a19296949a821d0bc65b8c38f8c`，本地 `HEAD`、`origin/main` 一致。
- ✅最终门禁为 Lint 0 error / 0 warning；Build 成功，主 JavaScript 237.65KB、gzip 76.49KB；三套既有 e2e 3/3 PASS，聊天持久化与导航回归测试 PASS。
- 📊生产移动端 Lighthouse 为 Performance 97、Accessibility 100、Best Practices 100、SEO 100；LCP 1.405s、CLS 0、FCP 1.369s、TBT 143ms。
- ✅生产冒烟 9/9 通过：落地页与 Console、375/1440 导航、三道情景题至顾怀瑾90%首次相遇、DeepSeek真实回复、Function日志安全、刷新持久化、旧链接3/3重定向、Lighthouse、预算台账均通过。
- 🤖Netlify 环境变量已确认：`DEEPSEEK_API_KEY` 存在且仅服务端使用，`DEEPSEEK_MODEL=deepseek-v4-flash`，`DEEPSEEK_API_URL` 留空，作用范围为 All deploy contexts。
- 💰DeepSeek 用量台账基线为0.02元；50元为一级记录节点、100元为二级配置复核节点、135元为三级暂停新对话并上报节点，总预算上限150元。
- 🩹生产冒烟曾发现刷新聊天页无限渲染与 `/match↔/chat` 导航闭环两项P0，已由 `[D5-HOTFIX]` 提交修复；9月4日独立复验确认刷新后3条会话消息仍在、DeepSeek接口HTTP 200、返回首页后换乘与报告均可达。
- ⚠️8月31日本地受控 Lighthouse 的移动端 LCP ≈3.3s 仍作为历史已知问题保留；生产复测为1.405s，差异可能包含运行环境与测量噪声，不据此删除历史记录。
- ⚠️聊天页返回按钮行为已改为 `/home`，但无障碍名称仍为“返回上一页”；该P2语义问题不阻塞上线，建议后续改为“返回首页”。
- ✅监督人于2026年9月4日完成T5最终判定，BONDSHIFT正式上线成功，本轮28/28有效任务全部收口。
