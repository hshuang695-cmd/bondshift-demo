# BONDSHIFT 2026-09-02 部署上线日 PRD

| 项目 | 内容 |
|---|---|
| 文档版本 | v1.0（2026-09-02 17:00 签发，监督人） |
| 上位文档 | `docs/2026-08-27-31-prd-responsive-deploy.md` v1.4（第 6.2 节 9/2 排期、5.3 上线流程、5.4 冒烟清单） |
| 执行窗口 | 2026-09-02 17:00 — 24:00（剩余约 7 小时） |
| 执行者 | Codex（指令 A/B/C）+ 监督人（D5-2/D5-4/Safari 手测/冒烟人工项） |
| 当前基线 | commit `a62869a`（D4-5 归档，9/1 16:25）；工作区有 2 个未跟踪监督人文档 |
| 冻结状态 | ⚠️ **12:00 全量代码冻结已生效**：今日禁止一切业务代码修改，仅允许①阻断性缺陷修复（须监督人逐项授权）②文档/证据/测试脚本类改动 |
| 部署目标 | Netlify 站点 `bondshift-demo`（今日目标：完成冒烟验收，确认可上线状态） |

---

## 一、未完成任务清单核查（截至 9/2 17:00）

| # | 任务 | 优先级 | 状态 | 原截止时间 | 逾期/风险说明 |
|---|---|---|---|---|---|
| 1 | D5-1 最终门禁（三件套 + e2e + 9 档 72 项矩阵） | **P0** | ❌ 未执行（指令已于 9/1 签发 `docs/2026-09-01-d51-final-gate-prompt.md`，Codex 会话未触发） | 9/1 18:00 | **已逾期约 23 小时**；测量类任务不受代码冻结限制，今日必须补执行 |
| 2 | 9/1 日报 | P1 | ❌ 缺失 | 9/1 24:00 | 按 R-6 条款「无日报 = 当日任务未完成」已自动判定；随指令 A 一并补记 |
| 3 | D4-1-R Safari 15 手测（监督人） | P1 | ❌ 未执行（手册已签发 `docs/2026-09-01-d41r-safari-test-checklist.md`，无证据目录 `docs/evidence/after/safari/`） | 8/31→9/1 顺延两次 | **⚠️ 修复窗口已关闭**：代码冻结后发现的兼容问题只能登记 `baseline.md` 待办，无法本轮修复；建议压缩为 20 分钟要点抽查，如实记录 |
| 4 | D5-2 环境变量（监督人） | P0 | 🟡 大概率已完成（待复核确认） | 9/2 12:00 | **重要事实**：9/1 生产接口实测（`bondshift-demo.netlify.app/api/chat`）3 次调用中 2 次返回 HTTP 200 真实模型回复——`chat.mjs` 在未配置 key 时会返回 503「尚未配置」，故可推断 `DEEPSEEK_API_KEY` **已在 Netlify 生产环境配置**。D5-2 从「配置」降级为「复核确认」 |
| 5 | D5-3 生产冒烟 9 项（监督人 + Codex） | **P0** | ❌ 未开始 | 9/2 | 上线前最后一道人工关卡；可脚本化 2 项交 Codex |
| 6 | D5-4 预算台账（监督人） | P1 | ❌ 未开始 | 9/2 | 纯记录类，10 分钟 |
| 7 | D5-5 文档收尾 + 9/2 日报（Codex） | P1 | ❌ 未开始 | 9/2 收工 | 依赖 D5-1/D5-3 结果 |
| 8 | 两份监督人手册归档 commit | P2 | 🟡 未跟踪状态（`docs/2026-09-01-*.md` × 2） | 随今日 commit | 并入指令 A 的提交清单 |

**已完成不再重做**（禁止回退）：D1 全批、D4-2（`1cd2b09`）、R1 SEO（`a53d81c`）、R2 LCP fail-fast（`4372442`）、R3 对比度（`9b98ab5`）、D4-5 + 页面级视觉冻结（`a62869a`）。LCP 3.2s 为已登记已知问题（`baseline.md`），不阻塞上线。

---

## 二、今日目标

**一条主线**：完成 D5-1 门禁补执行 → D5-2 复核 → D5-3 生产冒烟 → D5-5 收尾，使 `bondshift-demo` 达成「可上线确认」状态并归档全部证据。

**今日不做**：任何 LCP/性能再优化、任何视觉调整、停车场项（V-6/V-7/D1-9/D5-6）、依赖变更、推送远端（除非监督人明确授权 `git push` 触发部署）。

---

## 三、任务拆解 · 验收标准 · 优先级排序

| 顺序 | 任务 | 执行方 | 验收标准 | 预计耗时 |
|---|---|---|---|---|
| ① | **D5-1 最终门禁补执行 + 9/1 日报补记 + 手册归档**（指令 A） | Codex | 三件套 0 错误全 PASS；9 档 × 8 页 = 72 项零溢出矩阵全过（截图入 `matrix/`）；9/1 日报含逾期说明；`git status` 干净 | 40–60 min |
| ② | **D5-2 环境变量复核**（监督人自办） | 监督人 | Netlify 后台确认 `DEEPSEEK_API_KEY` 存在且未被改动（依据 9/1 实测可推断已配置）；记录配置时间与尾号 | 5 min |
| ③ | **D5-3 生产冒烟**：Codex 跑脚本化 2 项（指令 B）+ 监督人跑人工 7 项 | 双方 | 5.4 清单 9/9 打勾；任一失败 → 判定是否阻断性（是 → 授权修复后复测；否 → 记 baseline.md 待办并在日报声明） | Codex 15 min / 监督人 25 min |
| ④ | **D4-1-R Safari 压缩手测**（监督人，可选但强烈建议） | 监督人 | 按 `docs/2026-09-01-d41r-safari-test-checklist.md` 执行四点（顺序 3→1→4→2），记录单回填；❌ 项登记待办（冻结期不修复） | 20 min |
| ⑤ | **D5-4 预算台账**（监督人） | 监督人 | DeepSeek 用量台账标注 50/100/135 元三级节点（总上限 150 元），存 `docs/baseline.md` 或独立台账文件 | 10 min |
| ⑥ | **D5-5 文档收尾 + 9/2 日报**（指令 C） | Codex | `baseline.md` 对比目标状态更新；9/2 日报含冒烟 9 项结果与上线结论；PRD 回填 9/2 执行结果表（进度 27/28 或如实口径） | 15 min |

**上线判定规则（18:00 前由监督人拍板）**：
- 冒烟 9/9 全过 → **可上线确认**，D5 主线收口；
- 冒烟仅非阻断项失败（如 LCP 已知问题、Safari 兼容项）→ **带已知问题上线**，待办记 `baseline.md`，日报声明；
- 出现阻断性失败（白屏、主流程断裂、密钥泄露）→ 停止，按 5.6 回滚预案，明日修复窗口处理。

---

## 四、依赖关系

```
指令A（D5-1 门禁）──┐
                     ├──> D5-2 复核（监督人，随时）──> 指令B（D5-3 脚本化冒烟）──> 监督人冒烟 7 项 ──> 上线判定
Safari 手测（并行）──┘                                                          └──> 指令C（D5-5 收尾，依赖①③结果）
```

指令 A 必须最先发（门禁绿是冒烟的前提）；D5-2 与 Safari 手测可并行；指令 C 最后发。

---

## 五、Codex 执行指令（按序发送，直接复制）

### 指令 A · D5-1 最终门禁补执行（P0，先发）

> **【任务背景】** 你 9/1 的 D5-1 最终门禁未执行（会话未触发），现按 `docs/2026-09-01-d51-final-gate-prompt.md` 原指令补执行。两处时间点变更：① 当前已过 9/2 12:00 **全量代码冻结**，矩阵若发现溢出**只登记不修复**（逐档记录后继续，最后汇总报告）；② 日报为 9/1 补记，须在开头注明「9/2 补记，逾期原因：Codex 会话未触发」。
>
> **【具体要求】** 严格按 `docs/2026-09-01-d51-final-gate-prompt.md` 的二～五节执行（三件套 → 9 档 72 项矩阵 → 9/1 日报补记 → 主 PRD 6.1/6.2 回填），以下仅为**覆盖性变更**：
> 1. 提交命令更新为（新增两份监督人手册归档）：
>    ```bash
>    git add tests/ docs/daily/2026-09-01.md docs/2026-08-27-31-prd-responsive-deploy.md docs/evidence/after/responsive/ docs/2026-09-01-d41r-safari-test-checklist.md docs/2026-09-01-d51-final-gate-prompt.md
>    git commit -m "[D5-1] 最终门禁（9/2补执行）：三件套+e2e+9档全页矩阵72项零溢出，I-6收口；归档Safari手测与门禁指令文档"
>    ```
> 2. 9/1 日报「次日计划」小节改为写 9/2 实际剩余计划（D5-2 复核 / D5-3 冒烟 / D5-4 / D5-5）。
> 3. 主 PRD 6.2 节 9/1 表格补执行结果时，如实标注「9/2 补执行」。
>
> **【涉及文件范围】** 允许：`tests/`（新增矩阵脚本）、`docs/daily/2026-09-01.md`（新）、主 PRD 6.1/6.2、`docs/evidence/after/responsive/`、两份 `docs/2026-09-01-*.md`（仅随 commit 归档，内容已冻结不得改动）。禁止：`src/`、`netlify/`、`netlify.toml`、`index.html`、`package*.json`、`vite.config.ts`、`tsconfig*`。
>
> **【验收条件】** `git status --porcelain` 为空；`git log --oneline -2` 顶部为 `[D5-1]` commit（基于 `a62869a`）；矩阵 72 项全 PASS（或溢出项如实登记清单）；回报：三件套结果 + 矩阵汇总表 + commit 哈希 + `git show --stat`。异常即停条款不变。

### 指令 B · D5-3 生产冒烟脚本化项（P0，指令 A 回报绿后发）

> **【任务背景】** 生产站点 `https://bondshift-demo.netlify.app/` 进入 D5-3 冒烟。你负责 5.4 清单中 2 项可脚本化验收，结果供监督人合并判定。
>
> **【具体要求】**
> 1. 旧链接重定向验证：用 playwright 依次访问 `/vr`、`/voice`、`/compare`，断言最终 URL 均为 `/home` 且页面渲染出落地页特征元素；结果三行汇总。
> 2. Lighthouse 生产 URL 移动端复测：`npx lighthouse https://bondshift-demo.netlify.app/ --preset=perf --form-factor=mobile --screenEmulation.mobile --output=json --output-path=docs/evidence/after/production/lighthouse-prod-mobile.json`（目录不存在则新建）。判定线：Performance ≥85、A11y/BP/SEO ≥90（LCP 按 fail-fast 决议豁免，如实记录即可）。
> 3. 不修改任何文件（除上述新增 JSON 与必要的目录创建）。
>
> **【涉及文件范围】** 仅 `docs/evidence/after/production/`（新目录 + 1 个 JSON）。
>
> **【验收条件】** 回报格式：重定向 3 项 PASS/FAIL 表 + 生产 Lighthouse 五指标（Perf/A11y/BP/SEO/LCP）。任何一项 FAIL → 停止报告，不得尝试修复。**禁止 push、禁止在 Netlify 后台做任何操作。**

### 指令 C · D5-5 文档收尾 + 9/2 日报（P1，冒烟结果出来后发，发送时把两条横线处替换为实际结果）

> **【任务背景】** 今日（9/2）为部署上线日。D5-1 门禁已于 9/2 补执行（结果：＿＿＿），D5-3 生产冒烟已完成（结果：＿＿＿）。现执行 D5-5 收尾。
>
> **【具体要求】**
> 1. 更新 `docs/baseline.md`：新增「上线基线（2026-09-02）」小节——生产 Lighthouse 五指标、冒烟 9 项结果、已知问题清单（移动端 LCP ≈3.2s；Safari 手测结论：＿＿＿）、待办（LCP 优化候选：主包代码分割、createLucideIcon chunk 按页分割——D5 后专项）。
> 2. 新建 `docs/daily/2026-09-02.md`（结构沿用 8/31）：今日目标（D5 主线收口）、执行记录（D5-1 补执行 / D5-2 复核 / D5-3 冒烟 9 项 / D5-4 台账 / D5-5）、问题与处理（含 9/1 逾期说明）、今日结果（PRD 进度 ＿＿/28）、上线结论（可上线 / 带已知问题上线 / 暂缓，及依据）。
> 3. 回填主 PRD 6.2 节 9/2 表格执行结果（仿 8/29 格式）。
> 4. 提交：
>    ```bash
>    git add docs/baseline.md docs/daily/2026-09-02.md docs/2026-08-27-31-prd-responsive-deploy.md docs/evidence/after/production/
>    git commit -m "[D5-5] 上线收尾：冒烟9项结果与上线基线归档，9/2日报"
>    ```
>
> **【涉及文件范围】** 仅 `docs/baseline.md`、`docs/daily/2026-09-02.md`（新）、主 PRD 6.2、`docs/evidence/after/production/`。
>
> **【验收条件】** `git status --porcelain` 为空；回报 commit 哈希 + 日报中「上线结论」原文。异常即停条款不变。

---

## 六、监督人任务清单（无需 Codex）

| 任务 | 操作要点 |
|---|---|
| D5-2 环境变量复核（5 min） | Netlify 后台 → Site configuration → Environment variables：确认 `DEEPSEEK_API_KEY` 存在、记录配置时间与值尾 4 位；若意外缺失 → 按保守流程补配并触发重新部署，冒烟顺延 |
| D5-3 冒烟人工 7 项（25 min） | 按主 PRD 5.4 清单逐项打勾：落地页渲染 / 双形态导航 / 情景题主流程 / 真实 AI 回复（注意 fallback 横幅是「降级信号」）/ Function 日志无密钥泄露 / 刷新后聊天记录持久化 / 预算节点标注 |
| D4-1-R Safari 压缩手测（20 min，可选） | `npm run preview -- --host 127.0.0.1 --port 4173` 后按手册顺序 3→1→4→2 执行；❌ 项只登记（冻结期不修复） |
| D5-4 预算台账（10 min） | DeepSeek 平台查当前用量，在台账标注 50/100/135 元三级节点与当前水位 |
| 上线判定 | 汇总 Codex 两份回报 + 自测结果，按第三节判定规则拍板，结论口述转指令 C |

## 七、风险与约束

1. **代码冻结已生效**：一切 `src/` 改动须监督人书面授权且仅限阻断性缺陷；其余一律「只登记」。
2. **未推送纪律延续**：截至现在本地 17 个 commit 未推送；是否 `git push` 触发 Netlify 生产部署由监督人在上线判定时单独决定，Codex 无权推送。
3. **逾期根因**（供复盘）：D5-1 逾期 23 小时与 8/30 白天空转同型——Codex 会话未触发；建议 D5 收口后建立「每日固定时间发 Codex 指令」的触发习惯。
4. LCP 已知问题、Safari 待发现项、停车场项均不阻塞本次上线判定。
